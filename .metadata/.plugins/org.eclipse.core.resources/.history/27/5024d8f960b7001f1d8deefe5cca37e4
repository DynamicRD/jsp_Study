function idCheck(id) {
	let value =	document.regForm.id.focus();
	if (value === "") {
		alert("아이디를 입력해 주세요.");
	} else {
		alert("아이디를 입력하셨습니다.");
		url = "idCheck.jsp?id=" + id;
		window.open(url, "post", "width=300,height=150");
	}
}